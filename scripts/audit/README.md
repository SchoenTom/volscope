# scripts/audit/

Code + design-drift audits.

- `audit_design_drift.py` — semantic-drift detector (catches when UI deviates from PROMPT.md spec).
- `maturity_check.py` — scoring of overall project maturity (used by the Maturity Loop).
